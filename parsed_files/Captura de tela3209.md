The image shows a web application interface for managing users in a construction management system (SGO). A modal window titled "Novo Usuário" (New User) is open, allowing for the creation of a new user. The fields to be filled are: "Nome completo" (Full Name) with "Israel" pre-filled, "Nome de usuário" (Username) with "@israe2." pre-filled and a note indicating it's used for login with letters, numbers, and periods, and "Senha" (Password) with "123456" pre-filled. The "Perfil / Função" (Profile / Function) section allows selection from options like "Engenheiro" (Engineer), "Mestre de Obras" (Foreman/Master Builder), "PCP", and "Almoxarife" (Storekeeper). "Engenheiro" is currently selected. There are also "Cancelar" (Cancel) and "Criar Usuário" (Create User) buttons.

In the background, the main page displays "Usuários" (Users) with "0 usuários ativos" (0 active users) and a section explaining how access works: the manager creates the user with a username and password, and the engineer/foreman logs in using their username and password without needing an email, with each user accessing only the projects linked to them. There is also a notification at the top right indicating "email rate limit exceeded". A section for "1 usuário inativo" (1 inactive user) is also visible. The left sidebar shows navigation options for different modules of the system.

The main language detected is Portuguese.