Failed to create user: Database error creating new user.

A modal is open to "Create a new user" with fields for "Email address" (pre-filled with "master@sgo.dev") and "User Password". There is also a checkbox for "Auto confirm user?" with the explanation that "A confirmation email will not be sent when creating a user via this form." A "Create user" button is present at the bottom of the modal.