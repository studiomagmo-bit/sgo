The image displays the "Actions secrets and variables" settings page for a GitHub repository named "sgo". It explains that secrets are encrypted and used for sensitive data, while variables are plain text for non-sensitive data. The page shows sections for "Environment secrets" (which currently has no secrets) and "Repository secrets". Under "Repository secrets", two secrets are listed: "NEXT_PUBLIC_SUPABASE_ANON_KEY" and "NEXT_PUBLIC_SUPABASE_URL", with their last updated times indicated. There is also a button to add a "New repository secret".